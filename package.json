{
  "name": "react-p5",
  "private": true,
  "scripts": {
    "build": "turbo run build lint --filter=react-p5",
    "dev": "turbo run dev --filter=nextjs-example",
    "publish-packages": "turbo run build lint --filter=react-p5 && changeset version && changeset publish"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ]
  },
  "devDependencies": {
    "turbo": "^1.5.6"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.0"
  },
  "version": "0.0.2",
  "license": "MIT"
}
